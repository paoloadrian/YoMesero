<%= form_for(@item_ranking) do |f| %>
  <% if @item_ranking.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item_ranking.errors.count, "error") %> prohibited this item_ranking from being saved:</h2>

      <ul>
      <% @item_ranking.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :rate %><br>
    <%= f.number_field :rate %>
  </div>
  <div class="field">
    <%= f.label :item_id %><br>
    <%= f.text_field :item_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
